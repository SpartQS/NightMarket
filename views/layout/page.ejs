<!DOCTYPE html>
<html lang="ru">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/stylesheets/style.css">
  <!-- jQuery and Bootstrap JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    .navbar {
      background-color: #0C0032;
      padding: 15px;
      min-height: 70px;
    }

    .navbar-brand,
    .navbar-nav .nav-link {
      color: #ffffff;
      font-weight: bold;
    }

    .navbar-nav .nav-link:hover {
      color: #7e7e7e;
    }

    .btn-outline-white {
      border-color: #ffffff;
      color: #ffffff;
    }

    .btn-outline-white:hover {
      background-color: #ffffff;
      color: #0C0032;
      border-color: #ffffff;
    }

    .navbar-toggler-icon {
      background-image: url("data:image/svg+xml;charset=utf8,%3Csvg viewBox='0 0 32 32' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke='rgba(255, 255, 255, 1)' stroke-width='2' stroke-linecap='round' stroke-miterlimit='10' d='M4 8h24M4 16h24M4 24h24'/%3E%3C/svg%3E");
    }

    .profile-icon {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      overflow: hidden;
    }

    .profile-icon img {
      width: 100%;
      height: auto;
    }

    .nav-item {
      display: flex;
      align-items: center;
    }

    .navbar-nav {
      align-items: center;
    }

    .btn-logout {
      background-color: transparent;
      color: #ffffff;
      border: 2px solid #ffffff;
      padding: 10px 0;
      font-size: 16px;
      border-radius: 5px;
      cursor: pointer;
      width: 70px;
      text-align: center;
      white-space: nowrap;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .btn-logout:hover {
      background-color: #ffffff;
      color: #0C0032;
      border: 2px solid #ffffff;
    }
  </style>
</head>

<body class="bg-light">

  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid"> <!-- Было просто .container -->
      <a class="navbar-brand d-flex align-items-center" href="/">
        <img src="/images/moon.png" width="50px" height="50px" class="me-2">
        Nightmarket
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item me-3">
            <a class="nav-link" href="/">Главная</a>
          </li>
          <% if(user && user.role !==null && user.role===0) { %>
            <li class="nav-item me-3">
              <a class="nav-link" href="/cart">Корзина</a>
            </li>
            <% } %>
              <% if(user && user.role !==null && user.role===1) { %>
                <li class="nav-item me-3">
                  <a class="nav-link" href="/reports">Отчеты</a>
                </li>
                <li class="nav-item me-3">
                  <a class="nav-link" href="/orders_page">Заказы</a>
                </li>
                <% } %>
        </ul>

        <ul class="navbar-nav">
          <% if(user && user.role !==null && user.role===0) { %>
            <li class="nav-item">
              <a class="nav-link" href="/profile">
                <div class="profile-icon">
                  <img src="/images/person.png" alt="Профиль">
                </div>
              </a>
            </li>
            <% } %>
              <% if(user) { %>
                <li class="nav-item">
                  <form action="/logout" method="post" class="ms-3">
                    <button type="submit" class="btn btn-logout">Выйти</button>
                  </form>
                </li>
                <% } else { %>
                  <li class="nav-item">
                    <a class="btn btn-outline-white me-2" href="/login">Вход</a>
                  </li>
                  <li class="nav-item">
                    <a class="btn btn-outline-white" href="/register">Регистрация</a>
                  </li>
                  <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-5">
    <%- body %>
  </div>

</body>

</html>